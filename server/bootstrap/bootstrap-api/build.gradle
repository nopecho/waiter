dependencies {
    implementation(project(':core::interface:interface-reactive-web'))

    implementation(project(':core:infra:infra-reactive-redis'))
    implementation(project(':core:infra:infra-reactive-mongo'))
}