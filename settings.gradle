rootProject.name = "waiter"
include('bootstrap',
        'bootstrap:bootstrap-worker',
        'bootstrap:bootstrap-api')

include('core',
        'core:domain',
        'core:application',
        'core:infra',
        'core:infra:infra-reactive-redis')

include('commons',
        'commons:utils')